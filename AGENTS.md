# Repository Instructions

## AgentMemory

- Before starting an implementation task, search AgentMemory for memories related to VibeGame and the module being changed.
- When the user confirms a durable product rule, architecture decision, interface contract, workflow, or debugging conclusion, save a concise memory tagged with `VibeGame` and relevant module names.
- Do not save secrets, credentials, raw logs, transient status, or unverified guesses.
- Treat retrieved memories as context, not as the source of truth. Verify them against the current repository and the user's latest instructions.
- If AgentMemory is unavailable, continue from repository context and clearly state that memory retrieval or saving could not be performed.
